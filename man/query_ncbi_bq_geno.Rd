% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_ncbi_bq.R
\name{query_ncbi_bq_geno}
\alias{query_ncbi_bq_geno}
\title{Query antimicrobial genotype (MicroBIGG-E) data from NCBI Pathogen Detection BigQuery}
\usage{
query_ncbi_bq_geno(
  taxgroup,
  geno_subclass = NULL,
  geno_class = NULL,
  project_id = NULL
)
}
\arguments{
\item{taxgroup}{String specifying the organism group to filter on (e.g.,
"Pseudomonas aeruginosa"). See https://www.ncbi.nlm.nih.gov/pathogens/organisms/
for a list. Required.}

\item{geno_subclass}{(Optional) String or vector of strings specifying AMR
subclasses to filter on (e.g., "CARBAPENEM").}

\item{geno_class}{(Optional) String or vector of strings specifying AMR
classes to filter on (e.g., "BETA-LACTAM"). Ignored if \code{geno_subclass} is
specified.}

\item{project_id}{(Optional) Google Cloud Project ID to use for billing. If
NULL (default), looks for \code{GOOGLE_CLOUD_PROJECT} environment variable.}
}
\value{
A tibble containing genotype data with columns renamed to match
\code{import_amrfp()} expectations.
}
\description{
This function queries the \code{ncbi-pathogen-detect.pdbrowser.microbigge} BigQuery table to retrieve
genotype data. \strong{Note:} This function only returns genotypes for BioSamples that also have AST data.
}
\examples{
\dontrun{
# Query genotype data for Klebsiella pneumoniae samples that have AST data
geno_raw <- query_ncbi_bq_geno(
  taxgroup = "Klebsiella pneumoniae"
)

# Filter for carbapenem resistance genes and point mutations
geno_amrfp <- query_ncbi_bq_geno(
  taxgroup = "Klebsiella pneumoniae",
  geno_subclass = "CARBAPENEM"
)

geno <- import_amrfp(geno_amrfp, sample_col = "biosample_acc")
}
}
